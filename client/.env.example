# Variables de entorno del frontend (Vite)

# Teléfono de WhatsApp (público, se puede commitear)
VITE_WHATSAPP_PHONE=+56992834268

# Modo mock de chatbot (solo frontend):
# - true  => el servicio de chat NO llama a /chat/completions y responde con texto de prueba
# - false => usa la API real vía Pages Function (recomendado en producción)
VITE_CHAT_MOCK=true
